Create a new DWORD value named TaskbarDa in HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced.

Set the value data to 0 to hide it. To make it visible, set the value data to 1